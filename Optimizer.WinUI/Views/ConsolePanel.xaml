<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Optimizer.WinUI.Views.ConsolePanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:m="using:Optimizer.WinUI.Models">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>

        <!-- Dock header: title + pop-out / collapse -->
        <Grid Grid.Row="0" Padding="10,6"
              BorderThickness="0,0,0,1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
            <TextBlock Text="Console" FontWeight="SemiBold" VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="4">
                <Button x:Name="PopOutButton" Click="PopOut_Click" ToolTipService.ToolTip="Pop out">
                    <FontIcon Glyph="&#xE8A7;" FontSize="14"/>
                </Button>
                <Button x:Name="CollapseButton" Click="Collapse_Click" ToolTipService.ToolTip="Hide">
                    <FontIcon Glyph="&#xE8BB;" FontSize="14"/>
                </Button>
            </StackPanel>
        </Grid>

        <!-- ── Pane 1: Assistant (AI interface) ─────────────────────────── -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="ASSISTANT" FontSize="11" FontWeight="SemiBold"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" Padding="10,6,10,2"/>
            <ListView Grid.Row="1" ItemsSource="{x:Bind AssistantVM.Messages}" SelectionMode="None" Padding="6,0">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="m:ChatMessage">
                        <TextBlock Text="{x:Bind Text, Mode=OneWay}" TextWrapping="Wrap" Margin="0,2"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Grid Grid.Row="2" Padding="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" x:Name="InputBox"
                         Text="{x:Bind AssistantVM.Input, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         PlaceholderText="Ask the assistant…" KeyDown="Input_KeyDown"/>
                <Button Grid.Column="1" Content="Send" Margin="8,0,0,0"
                        Command="{x:Bind AssistantVM.SendCommand}"/>
            </Grid>
        </Grid>

        <!-- ── Pane 2: Activity (terminal output) ───────────────────────── -->
        <Grid Grid.Row="2" BorderThickness="0,1,0,0" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Padding="10,6,8,2">
                <TextBlock Text="ACTIVITY" FontSize="11" FontWeight="SemiBold"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right">
                    <ToggleSwitch OnContent="Verbose" OffContent="Verbose"
                                  IsOn="{x:Bind ConsoleVM.ShowVerboseLogs, Mode=TwoWay}"/>
                    <Button Content="Clear" Command="{x:Bind ConsoleVM.ClearCommand}"/>
                </StackPanel>
            </Grid>
            <ListView Grid.Row="1" ItemsSource="{x:Bind ConsoleVM.Lines}" x:Name="ActivityList"
                      FontFamily="Consolas" SelectionMode="None" Padding="6,0">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="m:ConsoleLine">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <TextBlock Text="{x:Bind TimeText}" Foreground="#6B7280"/>
                            <TextBlock Text="{x:Bind Glyph}"/>
                            <TextBlock Text="{x:Bind Text}" TextWrapping="Wrap"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</UserControl>
