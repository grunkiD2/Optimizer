# UI/UX + tuning-dybde-audit — 2026-06-13 (nat)

Metode: 6 parallelle audit-agenter (shell · Monitor · Optimize · Automate/Protect/Settings · tuning-dybde · dobbeltkode/divergens) målt mod brugerens to hårde UX-principper:
**P1: intet klikbart uden virkning. P2: intet resultat uden handlingsvej.**
Alle fund er verificeret med fil:linje. Status-kolonnen vedligeholdes efterhånden som der fixes.

## CRITICAL (vildleder brugeren eller bryder maskinejerskab)

| # | Fund | Hvor | Status |
|---|---|---|---|
| C1 | "Quiet PC"-preset aktiverer **High Performance** (det modsatte af sit løfte — på en noise-first-maskine) | BuiltInPresetsProvider.cs:81-86 | ✅ FIXET 13-06 |
| C2 | Event Log viser de 200 **ÆLDSTE** poster (EventLogReader default-retning) — dagens fejl findes aldrig | EventLogService.cs:27-31 | ✅ FIXET 13-06 |
| C3 | GPU-OC-skrivepanelet vises (sliders+Apply+Watchdog) men TryApply fejler ALTID (bevidst stub); "Reset to Default" melder falsk succes; det ærlige vendor-tool-fallback-kort skjules | GpuControlService.cs:12, NvApiGpuBackend.cs:215-235, TuningViewModel.cs:437-447 | ✅ FIXET 13-06 (capability-gating + ærlig reset) |
| C4 | **OptimizePowerSettings (powercfg /setactive) har INGEN maskingate** — nåbar fra Performance-kort, 6/10 presets, Command Center-quickaction, REST, scheduler; gentager den dokumenterede Lasso-incident | OptimizePowerSettingsHandler + WindowsOptimizerService | ✅ FIXET 13-06 (federations-gate i ApplyOptimizationAsync + preset-filter) |
| C5 | Toggle-OFF + "Undo Category" er dødt på ALLE 4 kategorisider (undo-match på Description.Contains(id) matcher aldrig) + kun 1 af N registry-værdier ville fortrydes | CategoryViewModelBase.cs:71-83, IUndoService.cs | ⏳ Batch 2 (OptimizationId-felt på UndoEntry + revert-all) |
| C6 | ApplyProfileAsync smider alle del-resultater væk og returnerer true — "Preset applied successfully" selv når ALT fejlede | WindowsOptimizerService.cs:186-188 | ⏳ Batch 2 (resultat-aggregering + "X af Y anvendt") |
| C7 | TuningService skriver powercfg-værdier ind i SCHEME_CURRENT (= Lassos aktive plan!) uden undo-capture; Revert = hardcodet Stock | TuningService.cs:43-77,225-229 | ⏳ Batch 2 (capture-before + ejerskabsgate) |
| C8 | MinimizeToTray-togglen er en attrap — Closing afslutter altid appen | MainWindow.xaml.cs:107-139 | ✅ FIXET 13-06 |
| C9 | PowerInsights + Fancontrol-sidernes tomtilstande henviser til settings (PpiEnabled, FancontrolStateDir) der IKKE findes i Settings-UI | PowerInsightsPage.xaml.cs:56, FancontrolPage.xaml.cs:57 | ⏳ Batch 2 (Settings-felter + inline enable-knap) |

## MAJOR — prioriteret kø (Batch 2-3)

**Skall/affordance:**
- Menulinje mangler helt (Filer/Vis/Værktøjer/Hjælp) — Ctrl+K-omnibox, Ctrl+`-konsol, IUndoService og IReportService er uopdagelige. Konkret menustruktur står i shell-agentens fix-felt.
- Konsol/chat: ingen kopi/markering/kontekstmenu; ConsoleLine.Color bindes aldrig (fejl ligner alt andet); LiveDot er altid-grøn fake.
- Assistent: LoadSessionAsync kaldes aldrig (hele sessionspersistensen død, feedback m. null-sessionId); ClearCommand + NeedsApiKey ubundet; ConfirmHandler kapres permanent efter pop-out; Pop out-knap i pop-out er død.
- Tray "Run Quick Cleanup" gør noget ANDET end Command Centers "Run Cleanup" (profil-heuristik vs ClearTemporaryFiles) + stille no-op.
- Command Center: Fancontrol-kortets ALARM/STALE-rækker og Automation-kortets PAUSED/OFF-rækker er inerte (ingen naviger/toggle); StatTiles har HoverLift uden klik.

**Inerte resultater (P2-brud):**
- Power-drainere/drift-anomalier: ingen ekskludér/åbn/kopiér (PpiProcessExclusions findes men er uredigerbar i UI).
- Event-log Error-rækker: ingen kopiér/Event Viewer/søg-på-ID/kendte-fix.
- Hardware: SensorUnavailableReason renderes aldrig (Live Sensors forsvinder ordløst); Export uden CanExecute; ingen kopi af serienumre/MAC.
- Fancontrol live-log: trimmede linjer uden tooltip/kopi; sentinel-issues kan ikke foldes ud.
- Disk-health rød badge uden "kør CHKDSK på dette drev"-kobling; Devices Status=Error uden handling.

**Stille fejl (gennemgående mønster):** service/startup/DNS/privacy/priority-handlinger ignorerer bool-retur (typisk manglende elevation) — toggle hopper tilbage uden besked. Fix-mønster: DevicesPage har allerede det rigtige (revert + InfoBar) — kopiér det.

**Stub/fake:** NetworkPage LATENCY-tile permanent N/A (live-monitor findes på samme side!); DOWNLOAD/UPLOAD frosset snapshot stylet som live; SystemPage "Sleep"-tile hardcodet; "Laptop-friendly"-badge på ALLE preset-kort (binding mangler); "SYSTEM HEALTH"-ringen er ren invers-utilization (omdøb til HEADROOM eller bland ægte sundhed ind); Intel Max-preset lover PL1/PL2 der aldrig skrives.

**Dobbeltkode/divergens (samlingsplan):**
1. CPU/GPU-temp: Optimizer viser "CPU Package"/"første GPU-temp", hjernen "Core Max"/"GPU Core" — to forskellige °C på samme skærm. → Federations-flader viser hjernens tal; navngiv sensoren ellers.
2. Staleness: 20 s (MC) / 30 s (Optimizer) / 60 s (hjernens fgwatch-krav) — producent bør deklarere cadenceSec i state-filerne; konsumenter beregner k×cadence.
3. CorsairLink-tap parses af både hjerne og MC med forskellig regex/sanity → coolant kan vises med 3 forskellige tal. → Hjernen ejer coolant (brain_state.cool); MC's kort skal læse den; tap kun til per-blæser-RPM.
4. MC's profil-knapper bygges af chroma.json + hardcodet fallback — skal læse profiles\profiles.json (datamodellen).
5. Gaming-klassen defineret to steder (Optimizers hardcodede sæt vs hjernens HINT_FLOORS≥46-regel) → udled af brain_config.json eller flyt til profiles.json som felt.
6. CPU-failsafe 90° hardcodet vs hjernens ALARM_CPU=96 (og forskellig sensor); disk-fuld 95 % vs 90/95 i diagnostik-plugin → fælles tærskler.
7. To undo-systemer: ChangeSetService (rig, SQLite, GroupId) er DI-registreret men har nul forbrugere; alt kører på flad UndoService → vælg ét (GroupId-modellen løser også "Revert profile river ALT ned"-buggen).
8. fgwatch stopped-flag ignoreres af Optimizer (død daemon ligner config-valg; RestartFgwatchAsync findes).
9. 5 færdige handlers (Autoplay/Transparency/AccentTitleBars/UpdateUX/UsbNotifications) er ikke på NOGEN kategoriside.
10. FancontrolStateDir uden ExternalSensorServerUrl → in-proc LHM-driver ved siden af :8085 (dokumenteret incident) — guard mangler.
11. v-feltet (R2) parses men håndhæves ikke fail-closed; ParseTelemetryLine læser slet ikke v.

**Sikkerheds-/adfærdsfund:** "Safe Tune"-quickaction sletter ALLE HKCU-autostart-entries via preset-clean (med undo, men uden varsel); stresstest kører uden termisk watchdog når LHM er nede (uden advarsel!); Ultimate Performance-knappen duplikerer planen ved hvert klik.

## Batch-plan

- **Batch 1 (✅ LEVERET 13-06 nat, `718167c`):** C1, C2, C3, C4, C8 — de fund der aktivt vildleder eller bryder ejerskab, med små skarpe diffs.
- **Batch 2 (✅ LEVERET 13-06, `6792f31` + `4e4e789`, 599/599):** C5 (undo-by-id), C6 (resultat-aggregering), C7 (TuningService-capture), C9 (Settings-felter), C10 (Recommendations-knap+filter), C12 (History-undo), C13 (Learning opretter regel), stille-fejl-InfoBars på 4 kategorisider, 5 forældreløse handlers, MetricsRefresh wired.
- **Batch 3 (UI-affordance — NÆSTE, ikke startet):** se "▶ BATCH 3 — HANDOFF" nedenfor.
- **Batch 4 (konsolidering):** divergens-listen 1-11 (én sandhed pr. værdi, jf. MACHINE-OWNERSHIP), stub-tiles fjernes/bindes (LiveDot-fake, ConsoleLine.Color ubundet, HEALTH-ring, LATENCY/Sleep/Laptop-badge), assistent-persistens (LoadSessionAsync er DØD) + pop-out ConfirmHandler-fix, ChangeSet-beslutningen.

## ▶ BATCH 3 — HANDOFF (ny samtale starter HER)

**Scope (UI-affordance, ingen logik-løgne — alt er "klikbart-men-mangler-vej" eller "resultat-uden-handling"):**
1. **Menulinje** i `MainWindow.xaml` (AppTitleBar, Grid.Row=0 — i dag kun logo+titel). Gør Ctrl+K-omnibox + Ctrl+`-konsol + IUndoService + IReportService opdagelige. Struktur fra shell-agenten:
   - **Filer:** Eksportér rapport (IReportService) · Indstillinger (naviger Settings) · Afslut
   - **Vis:** Konsol-dock (Ctrl+`) · Pop out · Tema/backdrop · Maksimér
   - **Værktøjer:** Run Cleanup · Profil-presets (ProfileService.BuiltInPresets) · Re-detect context · Fortryd seneste optimering (IUndoService) · Stress test · Network speed test · System repair
   - **Hjælp:** Genveje (Ctrl+K / Ctrl+`) · Vis app.log · Om
2. **Kontekstmenuer (ContextFlyout)** på alle række-lister: power-drainere (→ Exclude process = tilføj til `PpiProcessExclusions`, Copy details), event-log (Copy details, Open Event Viewer `eventvwr.msc`, Search event-ID), processer (End task, Open file location), services (Open services.msc, vis RecommendationReason), startup (Open file location), store filer (Delete, Copy path), devices (Restart device, Enhedshåndtering), fancontrol-log (Copy line, Open events.jsonl), konsol/chat (Kopiér linje/alt, Gem log — sæt også `IsTextSelectionEnabled`).
3. **Inert-result-koblinger:** C11 Diagnostics-fund → naviger via `CommandCenterPage.CategoryRoutes`/`CategoryTag(FindingCategory)` (genbrug — ÉN sandhed) · disk-health rød badge → "Schedule CHKDSK" m. drevet forvalgt · Fancontrol-kort (Command Center) ALARM/STALE-rækker → naviger "Fancontrol" + Ack · bottlenecks → End Process/naviger Performance.

**Fremgangsmåde (samme som virkede for Batch 2):**
1. Recon FØRST: en parallel read-only Workflow med ~3 Explore-agenter der kortlægger (a) MainWindow/konsol-dock/tray for menulinjen, (b) alle række-list-XAML + deres DataTemplates for ContextFlyout, (c) de inerte fund + eksisterende navigations-/exclusion-API'er — hver med ORDRETTE kodeuddrag + linjenr.
2. Implementér SEKVENTIELT (IKKE parallelle agenter — delte filer), én klynge ad gangen: menulinje → kontekstmenuer → inert-koblinger. **Build + test mellem hver klynge.**
3. Commit pr. klynge. Screenshot-verificér menulinjen + mindst én kontekstmenu live (Windows-MCP).

**Konventioner/fælder (lært denne session — overhold):**
- **STOP appen før build:** `Stop-Process -Name Optimizer.WinUI -Force` ellers fejler build på låst `apphost.exe`.
- Build: `dotnet build L:\Projects\Optimizer.WinUI\Optimizer.WinUI.csproj -c Debug -p:Platform=x64 --nologo`. Test: `dotnet test L:\Projects\Optimizer.WinUI.Tests\...` (baseline **599/599**).
- **ViewModels er UI-type-fri** (ingen Microsoft.UI.Xaml-typer) — brug converters (se `BoolToInfoBarSeverityConverter` som mønster).
- Hub-aware navigation: `IPageNavigator.NavigateTo(tag)` + `CommandCenterPage.CategoryRoutes`/`CategoryTag` — ALDRIG dupliker route-mappen.
- Ejerskab står fast: Optimizer rører ikke fans/power-plan/profiler (MACHINE-OWNERSHIP.md). Fancontrol-flader er read-only + gatede ctl-kommandoer.
- Når en VM-metode-signatur ændrer sig: tjek Moq-setups i Tests (5-param `CaptureRegistry`-fælden) + manuelle fakes.

**Kickoff-prompt til den nye samtale:** *"Læs `L:\Projects\docs\UI-AUDIT-2026-06-13.md` afsnittet '▶ BATCH 3 — HANDOFF' og gennemfør Batch 3 efter den beskrevne fremgangsmåde (recon → sekventiel implementering → build/test/commit pr. klynge)."*

## Automate + Protect + Settings (eftersendt fra genkørslen)

**4 yderligere CRITICALS:**
| # | Fund | Hvor | Status |
|---|---|---|---|
| C10 | Recommendations-sidens accent-primærknap ("Fix"/"Clean Up"/"Review") er DØD for alle anbefalinger undtagen privacy-preset'en (QuickAction==null → stille return) | RecommendationsViewModel.cs:136, RecommendationsService.cs:47-87 | ⏳ Batch 2 |
| C11 | Diagnostics-fund (Quick/Full Scan) er 100 % inert tekst — RECOMMENDATION-boks uden én knap/navigation | DiagnosticsPage.xaml:142-200 | ⏳ Batch 3 (kategori→side-mapping) |
| C12 | History's UNDO-knap markerer posten "Undone" SELV når intet blev rullet tilbage ("Always record", inkl. no-match/fejl) | HistoryViewModel.cs:224-243 | ⏳ Batch 2 (sammen med C5 undo-by-id) |
| C13 | Learning-sidens "Accept" på automation-forslag opretter ALDRIG en regel (sætter kun DB-status ingen læser) — succes-besked lyver | RuleSuggestionService.cs:100, LearningDashboardViewModel.cs:102-109 | ⏳ Batch 2 |

**Majors (uddrag):** Security-fradrag ("Enable BitLocker…") er rene tekstkort uden handling/link · Settings har TO døde felter (AccentColor + MetricsRefreshSeconds gemmes men forbruges aldrig) og MANGLER felterne der faktisk gater features (PpiEnabled, AutomationPaused, AutoApplyEnabled, AutoContextSwitch — jf. C9) · winget-upgrade afgør succes ved streng-scan af output i stedet for exit-kode og rydder listen optimistisk · Add Rule-dialogens Profile ID er fritekst uden validering (tastefejl = stille fejlende regel for evigt) · Recommendations-kategorifilteret mister permanent rækker (filtrerer den filtrerede liste — Diagnostics har master-liste-mønstret) · Maintenance-alerts har kun Dismiss (ingen undersøg-vej) + hardcodet rød badge · Driver-konflikter/bottlenecks viser PID/enhed uden end-process/Enhedshåndtering-handling.

**Minors:** rå True/False-tekster på Defender/firewall-status · regel-toggle viser "True"/"False" · TryParseTime → stille 00:00 · Reports valgt-tilstand usynlig · tomme CategoryIcons · Copy Token uden feedback.

**Positivt:** alle settings persisteres reelt; profiler/snapshots/regler og winget-listen er ægte implementeringer; Reports-generate/save/open virker; Diagnostics-filteret har det rigtige master-liste-mønster andre skal kopiere.
