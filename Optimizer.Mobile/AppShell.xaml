<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Optimizer.Mobile.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pages="clr-namespace:Optimizer.Mobile.Pages"
    Shell.BackgroundColor="{StaticResource BgColor}"
    Shell.ForegroundColor="{StaticResource TextColor}"
    Shell.TabBarBackgroundColor="{StaticResource ElevatedColor}"
    Shell.TabBarForegroundColor="{StaticResource AccentColor}"
    Shell.TabBarUnselectedColor="{StaticResource MutedColor}"
    Shell.TitleColor="{StaticResource TextColor}">

    <TabBar x:Name="MainTabBar">
        <ShellContent
            Title="Status"
            Route="dashboard"
            ContentTemplate="{DataTemplate pages:DashboardPage}"/>
        <ShellContent
            Title="Profiles"
            Route="profiles"
            ContentTemplate="{DataTemplate pages:ProfilesPage}"/>
        <ShellContent
            Title="Tips"
            Route="recommendations"
            ContentTemplate="{DataTemplate pages:RecommendationsPage}"/>
        <ShellContent
            Title="Settings"
            Route="settings"
            ContentTemplate="{DataTemplate pages:SettingsPage}"/>
    </TabBar>
</Shell>
